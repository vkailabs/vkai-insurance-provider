# Provider API base URL (vkai-insurance-provider-api, via Docker Compose)
VITE_VKAI_INSURANCE_PROVIDER_API_BASE_URL=http://localhost:4100

# Entra ID (Azure AD) — "VKAI Insurance Provider Portal" app registration
VITE_ENTRA_CLIENT_ID=
VITE_ENTRA_TENANT_ID=

# Security group object IDs used for frontend role detection (UI convenience only)
VITE_ENTRA_REVIEWER_GROUP_ID=
VITE_ENTRA_APPROVER_GROUP_ID=
